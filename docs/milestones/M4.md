# M4 —— Web：RPC 桥 + 会话列表 + composer + 流式 + tool 卡片

- **状态**：`draft`（占位；进入本 M 前定稿）
- **前置**：M3
- **概览**（requirements §9）：Web：RPC 桥 + 会话列表 + composer + 流式 + tool 卡片
- **验收要点**：浏览器里完成一次真实对话
- **教程**（§5.1）：`M4-web` — host↔client 桥：流式消息怎么到浏览器

> 定稿时机：**M3 收尾时**，由当次 session 的 agent 撰写并记入 notes（用户可审查）。
> 定稿内容：任务拆解（TDD 顺序）、详细验收清单、教程要求、完成后收尾动作；完成后状态改为 `proposed`。
