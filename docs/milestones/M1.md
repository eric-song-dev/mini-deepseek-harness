# M1 —— Session 事件词汇 + JSONL 持久化 + resume

- **状态**：`draft`（占位；进入本 M 前定稿）
- **前置**：M0
- **概览**（requirements §9）：Session 事件词汇 + JSONL 持久化 + resume
- **验收要点**：事件断言测试通过；崩溃恢复补 `turn/end`
- **教程**（§5.1）：`M1-event-log` — 事件日志与真源：为什么 append-only、崩溃恢复

> 定稿时机：**M0 收尾时**，由当次 session 的 agent 撰写并记入 notes（用户可审查）。
> 定稿内容：任务拆解（TDD 顺序）、详细验收清单、教程要求、完成后收尾动作；完成后状态改为 `proposed`。
