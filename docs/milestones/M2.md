# M2 —— LLM seam + 假 LLM + agent loop

- **状态**：`draft`（占位；进入本 M 前定稿）
- **前置**：M1
- **概览**（requirements §9）：LLM seam + 假 LLM + agent loop
- **验收要点**：假 LLM 跑通单轮 chat，全量落日志
- **教程**（§5.1）：`M2-llm-and-loop` — seam 与假 LLM：agent loop 怎么驱动会话

> 定稿时机：**M1 收尾时**，由当次 session 的 agent 撰写并记入 notes（用户可审查）。
> 定稿内容：任务拆解（TDD 顺序）、详细验收清单、教程要求、完成后收尾动作；完成后状态改为 `proposed`。
