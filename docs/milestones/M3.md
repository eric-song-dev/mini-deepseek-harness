# M3 —— tools（bash/fs）+ 工具调用循环

- **状态**：`draft`（占位；进入本 M 前定稿）
- **前置**：M2
- **概览**（requirements §9）：tools（bash/fs）+ 工具调用循环
- **验收要点**：工具事件入日志，循环多步正确
- **教程**（§5.1）：`M3-tools` — 工具注册与执行：工具事件如何进轨迹

> 定稿时机：**M2 收尾时**，由当次 session 的 agent 撰写并记入 notes（用户可审查）。
> 定稿内容：任务拆解（TDD 顺序）、详细验收清单、教程要求、完成后收尾动作；完成后状态改为 `proposed`。
